#!/bin/sh
# Compile a Theurgy app directory to a native adapter root.

case "${1-}" in
--help|--usage|-h)
  cat <<'USAGE'
Usage: compile-theurgy-app APP_DIR --target macos|linux|ios|android --out OUT_DIR
USAGE
  exit 0
  ;;
esac

set -eu

repo_dir=$(CDPATH= cd -- "$(dirname "$0")/.." && pwd -P)
exec "$repo_dir/scripts/theurgy-cargo" run --quiet --manifest-path "$repo_dir/Cargo.toml" --bin theurgy-runtime -- compile-app "$@"
